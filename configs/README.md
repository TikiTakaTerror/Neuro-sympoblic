# Configs

This folder will store reproducible experiment and model configuration files.

## Planned Layout

- `experiments/`: run-level configs for standard and controlled comparisons
- `models/`: model-specific configs or shared hyperparameter templates

## TODO

- TODO: Decide on a single config format in Phase 2 or Phase 3
- TODO: Keep config names aligned with `docs/experiment_matrix.md`
