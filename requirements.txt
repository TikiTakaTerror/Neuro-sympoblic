# Phase 2 core environment.
# These versions were installed successfully in a fresh local `.venv`
# on this machine using Python 3.10.

numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
matplotlib==3.10.8
jupyter==1.1.1
torch==2.11.0
torchvision==0.26.0

# External thesis dependencies are intentionally deferred:
# - `rsbench` will be handled as an external repository in Phase 3.
# - `LTNtorch` will be validated separately before the neuro-symbolic phase.
# - The official Concept Bottleneck repo uses a legacy stack, so the thesis
#   project will likely use an in-house CBM-style implementation instead.
